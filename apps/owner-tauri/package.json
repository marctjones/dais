{
  "name": "dais-owner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "smoke": "npm run build && node ../../scripts/owner-tauri-smoke.mjs",
    "tauri:run": "npm run build && cargo run --manifest-path src-tauri/Cargo.toml",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "typescript": "^5.9.3",
    "vite": "^7.2.7"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0"
  }
}
